# Evaluation Report

_Stub — regenerated by `pytest backend/tests/eval -m live` after the pipeline has run against the seed corpus._

## Entity extraction / resolution

- precision: —  recall: —  F1: —

## Contradiction detection

- precision: —  recall: —  F1: —

## How to regenerate

```bash
./scripts/dev-up.sh
./scripts/seed-demo.sh            # ingests + runs the pipeline with live Claude API
cd backend && source .venv/bin/activate
pytest tests/eval -m live         # writes a fresh eval_report.md at the repo root
```

Prompt-change policy: any edit to `backend/app/extraction/prompts.py` must include the
before/after diff of this file in the PR description.
